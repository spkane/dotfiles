Local Pi config (`~/.pi/agent/config.json`) includes packages: `pi-subagents`, `pi-mcp-adapter`, `pi-web-access`, `pi-hermes-memory`, `pi-lens`, `pi-goals`, `pi-routines`, `@trevonistrevon/pi-loop`. `@trevonistrevon/pi-loop` provides `/loop`, `/tasks`, and Loop/Task/Monitor tools; duplicate `/loop` commands can appear because `pi-routines` and `@trevonistrevon/pi-loop` both register `loop`. <!-- created=2026-06-25, last=2026-06-29 -->
§
`opentofu-core` (`/Users/spkane/dev/urun/core/opentofu-core`) convention from CLAUDE.md: implement exactly one canonical path; no silent fallbacks, alternate paths, stopgaps, env-gated downgrades, or “try X else Y”. If the canonical path cannot run, fail hard/loudly. <!-- created=2026-06-25, last=2026-06-29 -->
§
Pi memory SQLite search sync may fail when `better-sqlite3` under `/Users/spkane/dev/spkane/dotfiles/.pi/agent/npm/node_modules/` was built for a different Node ABI. The memory write can still save to Markdown, but search sync reports `NODE_MODULE_VERSION` mismatch; fix by rebuilding/reinstalling the module for the active Node version. <!-- created=2026-06-25, last=2026-06-25 -->
§
Tau repo (`/Users/spkane/dev/urun/tau`) uses pnpm. Full verification: `pnpm verify` (docs/checks, lint, typecheck, tests, build). Common targeted checks: `pnpm run lint`, `pnpm run typecheck`, `pnpm run test:build && node --test dist-test/tests/composer-layout.test.js` or `dist-test/tests/pi-extension-policy.test.js`. Do not commit runtime files under `.pi/loops/*.json`. <!-- created=2026-06-29, last=2026-06-29 -->
§
Pi memory writes in this environment can succeed to Markdown while SQLite search sync fails because `better-sqlite3.node` was compiled for a different Node module version. If memory save reports this warning, treat the memory as saved but search indexing may lag until the module is rebuilt. <!-- created=2026-06-29, last=2026-06-29 -->
§
Local Pi config includes recommended/installed extension packages: pi-subagents, pi-mcp-adapter, pi-web-access, pi-hermes-memory, pi-lens, pi-goals, pi-routines, and @trevonistrevon/pi-loop. @trevonistrevon/pi-loop provides scheduled/cron/one-shot loops plus /loop and /tasks commands. <!-- created=2026-06-29, last=2026-06-29 -->
§
Tau project (/Users/spkane/dev/urun/tau): the `vet` command may be blocked when Claude auth environment is unavailable; the suggested agentic fallback can time out, so report vet as blocked rather than claiming it passed. <!-- created=2026-06-29, last=2026-06-29 -->
§
User's local Pi package set (from ~/.pi/agent/config.json during Tau work) includes: pi-subagents, pi-mcp-adapter, pi-web-access, pi-hermes-memory, pi-lens, pi-goals, pi-routines, and @trevonistrevon/pi-loop; /loop commands come from @trevonistrevon/pi-loop. <!-- created=2026-06-29, last=2026-06-29 -->
§
Tool quirk: On this machine, plain `vet` may fail due to missing Anthropic authentication environment; use `vet --agentic` with local coding-agent CLIs when appropriate. Treat that failure as environment/tooling, not code failure. <!-- created=2026-06-29, last=2026-06-29 -->
§
Tau repo workflow (/Users/spkane/dev/urun/tau): use pnpm. `pnpm verify` runs docs:check, lint, typecheck, test, and build. Controller API docs are generated by `scripts/generate-controller-docs.cjs` into `docs/generated/controller-apis.md`; update the generator sources when adding/removing documented controller files. <!-- created=2026-06-29, last=2026-06-29 -->