Consolidated Pi/runtime quirks: `vet` may fail without Anthropic/API auth; `vet --agentic` can time out and is inconclusive unless it finishes with fresh output; do not combine `vet --agentic` with `--staged`. Keep transient Pi artifacts like `.pi/scheduled_tasks.lock` out of commits via local excludes. Pi memory writes may save Markdown while SQLite search sync fails if `better-sqlite3` was built for a different Node ABI (observed 147 vs required 137/146); rebuild/reinstall under the active Node before relying on `memory_search`. <!-- created=2026-06-24, last=2026-06-29 -->
§
CI/GitHub tooling quirks: One CLI GitHub action `Download a Workflow Job’s Logs (Redirect URL)` has fragile path-variable handling and can return 404 for accessible jobs; after One failures, prefer `gh run view --job <id> --log-failed`. GitHub Actions `astral-sh/setup-uv@v8` may fail to resolve; pin a concrete release such as `astral-sh/setup-uv@v8.2.0`. <!-- created=2026-06-25, last=2026-06-29 -->
§
skypilot-controller quirks: Pyright/LSP outside the `uv` env may report `pytest` missing even when `uv run pytest` passes; use a narrow `# type: ignore[reportMissingImports]` if needed and rerun `uv run ruff`/format plus diagnostics. Semgrep flags subprocess risks: use argv-based `subprocess.Popen([...], shell=False)` in Python, validate inputs, and for intentional Go `exec.Command` operator-configured paths add a narrow suppression comment with rationale. Runner assignment `/assign` may return before child side-effect files are written, so tests should poll/wait briefly before asserting those files. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] On the Tau repo, running `vet` directly failed because Anthropic credential environment variables were not set; attempting an agentic fallback was available via local Claude/Codex CLIs but timed out after 180s in this session. — Failed: `vet` requires configured Anthropic credentials unless using an agentic fallback; the fallback may be slow or hang and should not be treated as completed verification without fresh output. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] In /Users/spkane/dev/urun/tau, `vet` may fail when the required Claude/Anthropic auth credentials are not configured; the suggested agentic fallback timed out after 180s in this session. Prefer project verification commands unless credentials and a longer timeout are available. — Failed: The default `vet` auth prerequisite was missing, and the local-agent fallback did not finish within the timeout. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory writes on this machine may save to Markdown but fail SQLite search sync because better-sqlite3 was compiled for a different Node module ABI than the current Node version. Rebuild/reinstall the module if memory search indexing is needed. — Failed: Memory write reported a better-sqlite3 NODE_MODULE_VERSION mismatch during SQLite sync. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory writes may save successfully to Markdown while SQLite search sync fails with `better-sqlite3` Node ABI mismatch. Observed variants include active Node requiring `NODE_MODULE_VERSION` 146 or 137 while the installed module was built for 147; rebuild/reinstall better-sqlite3 under the active Node version to restore search indexing. — Failed: The memory tool returned success but warned that SQLite search sync failed due to `better-sqlite3` compiled for a different Node ABI. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Consolidated Pi/runtime quirks: `vet` may fail when Anthropic/API auth env vars are absent; `vet --agentic` can time out, so treat it as inconclusive unless fresh output completes. Pi artifacts like `.pi/scheduled_tasks.lock` may appear untracked; exclude locally instead of tracked ignore/config changes. Pi memory writes may save Markdown while SQLite search sync fails if `better-sqlite3` was built for a different Node ABI; rebuild/reinstall it under the active Node before relying on `memory_search`. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory writes can save to Markdown while SQLite search sync fails when better-sqlite3 was built for NODE_MODULE_VERSION 147 but current Node requires 137; memory write still reports success with a warning. — Failed: better-sqlite3 native module ABI mismatch during SQLite search sync after memory add. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] In skypilot-controller, Semgrep diagnostic `go.lang.security.audit.dangerous-exec-command.dangerous-exec-command` can flag the SkyPilot runner's operator-configured `exec.Command` path. If the path is intentionally argv-based and not shell-expanded, add a narrowly scoped suppression comment with rationale after auditing input provenance. — Failed: Semgrep flags non-static `exec.Command` in Go even for argv-based operator-configured subprocess paths; functional tests passed, but edited-file diagnostics remained blocked until an explicit suppression comment with rationale was added. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory SQLite sync may fail with better-sqlite3 Node ABI mismatch; memory writes still save to Markdown. Error example: `better_sqlite3.node was compiled against NODE_MODULE_VERSION 147; this Node requires 137/146`. Fix by rebuilding/reinstalling the module for the active Node version. — Failed: Memory writes save to Markdown but SQLite search sync can fail when better-sqlite3 was built for a different Node ABI; during this review it reported NODE_MODULE_VERSION 147 vs required 137 (previously also seen as required 146). Rebuild/reinstall better-sqlite3 for the active Node version. <!-- created=2026-06-29, last=2026-06-29 -->
§
[failure] skypilot-controller runner tests: assignment-agent `/assign` response can return before the payload subprocess has written side-effect files (e.g. env capture file). Do not assert those files immediately; wait/poll briefly for the expected file before asserting. — Failed: A worker-agent test asserted on a subprocess-written env file immediately after the assignment response. The agent can respond before the child process writes the file, so the test was flaky/failed. Fix was to poll/wait briefly for the file before asserting. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Tau repo: `pnpm verify` starts with `pnpm run docs:check`, which reads the hard-coded `sources` list in `scripts/generate-controller-docs.cjs`. If a documented controller source such as `src/main/scoped-runtime-coordinator.ts` is deleted, docs:check fails with ENOENT until the source list and generated docs are updated. — Failed: After deleting `src/main/scoped-runtime-coordinator.ts`, `pnpm verify` failed during docs:check: ENOENT opening that deleted file. Removing the stale source path from `scripts/generate-controller-docs.cjs` and regenerating docs fixed the immediate docs check. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory tool on this machine may save Markdown successfully but fail SQLite search sync because `better_sqlite3.node` was compiled for NODE_MODULE_VERSION 147 while the current Node requires 137. The memory write can still report success with a warning; search may not see the new entry until better-sqlite3 is rebuilt for the active Node. — Failed: Memory writes during background review returned: 'Saved to Markdown, but SQLite search sync failed' with a better-sqlite3 Node module version mismatch. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] In the Tau environment, `vet` may fail when provider authentication environment variables are absent, exiting code 2 with a message that no Anthropic auth token/key is set. Use an authenticated environment or an agentic fallback; the prior agentic fallback timed out after 180s. — Failed: The default vet invocation requires provider auth, and the attempted local agent fallback did not complete before timeout. <!-- created=2026-06-29, last=2026-06-29 -->
§
[tool-quirk] Pi memory writes can succeed to Markdown while SQLite search sync fails if better-sqlite3 was built for a different Node module ABI. Observed warning: better_sqlite3.node compiled for NODE_MODULE_VERSION 147 while current Node requires 137; memory is saved but search index may be stale until better-sqlite3 is rebuilt/reinstalled. — Failed: Native better-sqlite3 binary ABI mismatch with the Node version used by the memory tool. <!-- created=2026-06-29, last=2026-06-29 -->