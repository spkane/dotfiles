Agents repo architecture convention: agent subprocesses should not have unfettered GitHub write access. Agent-facing git access should go through the egress/proxy + lease/request flow (e.g. GIT_ASKPASS/internal git credential path). Platform-owned pod processes may use the dedicated agents-data PAT for persistence, but fixes must preserve this separation. <!-- created=2026-06-29, last=2026-06-29 -->
§
Agents dev-usw2 Kubernetes auth details: gateway pod env `GITHUB_PAT` comes from Kubernetes secret `agents-github-pat`, key `token`; Claude auth secret is `CLAUDE_AUTH_SECRET_ID=dev-usw2/agents/claude-auth` and stores `CLAUDE_CODE_OAUTH_TOKEN` plus issued-at metadata for Mode B auth. <!-- created=2026-06-29, last=2026-06-29 -->
§
Agents dev-usw2 outage lesson: gateway `data-seed` init can crashloop if the agents-data PVC `.git/config` remote embeds a stale HTTPS token and `git fetch origin` exits 403. Durable pattern: never embed tokens in remotes; use GIT_ASKPASS/current `agents-github-pat` secret, and keep scoped-sync fetch non-fatal so init does not block startup. <!-- created=2026-06-29, last=2026-06-29 -->
§
Agents repo Claude auth decision: use Mode B static `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token`, not rotating Claude.ai OAuth `.credentials.json`, to avoid pooled subprocess refresh-token races. `scripts/upload-claude-auth.sh --profile <profile> --cluster <cluster>` uploads it to `<cluster>/agents/claude-auth`; runtime bootstraps token from `CLAUDE_AUTH_SECRET_ID` and posts Slack rotation reminders near expiry. <!-- created=2026-06-29, last=2026-06-29 -->
§
Agents repo GitHub PAT diagnostic: fine-grained PATs can show `/repos` permissions `push: True` for the user while still lacking token `contents:write`. Verify true write scope by attempting a reversible GitHub API write (e.g. create/delete a throwaway ref) and checking `x-accepted-github-permissions`; `403 Resource not accessible by personal access token` means the PAT scope/secret must be replaced. <!-- created=2026-06-29, last=2026-06-29 -->